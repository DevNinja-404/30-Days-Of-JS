// use the map method to create a new array where each number is doubled and log the new array:
const num = [1, 2, 3, 4, 5];
const doubledNum = num.map((eachNum) => eachNum * 2);
console.log(doubledNum);
