// Access the first and last element of the array and log them to the console

const num = [1, 2, 3, 4, 5];
console.log(`The first element is : ${num[0]}`);
console.log(`The last element is : ${num[num.length - 1]}`);
