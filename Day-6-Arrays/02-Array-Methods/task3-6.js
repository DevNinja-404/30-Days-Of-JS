// task-3 : use the push method to add a new member to the end of the array and log the updated array

const num = [1, 2, 3, 4, 5];
num.push(6);
console.log(`After uisng push : ${num}`);

// task-4 : use the pop method to remove the last element from the array and log the updated array
num.pop();
console.log(`After using pop : ${num}`);

// task-5 : use the shift method to remove the first element from the array and log the updated array
num.shift();
console.log(`After using shift : ${num}`);

// task-6 : use the unshift method to add a new number to the bgining of the array :
num.unshift(1);
console.log(`After using unshift : ${num}`);
