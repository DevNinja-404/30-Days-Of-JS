// WAP that uses the ternary operator to check if the number is even or oddand log the result to the console

const num = 15;

num % 2 === 0 ? console.log(`${num} is even`) : console.log(`${num} is odd`);
